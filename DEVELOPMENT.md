# 開發與維護

本文件供規則、資料及發布維護者使用。一般使用方法以
`README.md`、`README.zh-CN.md` 及 `SKILL.md` 為準。

## 學習路徑

1. **讀者**：先讀 `SKILL.md` 的任務路由，再按任務讀一份
   `references/` 規則。使用查詢工具取得單一語料或詞條，不直接載入完整資料檔。
2. **審查者**：選定 `kind`、`style`、地區及專案規則，以人工判斷核對
   `chinese_lint.py` 的結果。靜態診斷是審查候選項，不代替語意判斷。
3. **規則維護者**：從可重現樣本建立最小規則、反例及測試。先修改規則的唯一
   資料來源，再更新必要的說明；不要在多份文件重複維護同一規則。
4. **發布維護者**：確認公開介面、兩份 README、版本、整合範例、授權及來源資料
   保持一致，完成發布檢查後才建立標籤及 GitHub Release。

## 迭代流程

1. 保存一個可公開的最小問題樣本，記錄文件類型、地區、預期結果及失敗條件。
   移除私人資料；不要把改寫後的句子當成原始樣本。
2. 在修改前重現問題，並先判斷缺口類型。
   缺口可能屬於觸發、工作流程、文字規則、術語、語料、詞庫或整合。不要用通用禁詞掩蓋特定語法或介面問題。
3. 實作最小規則。
   固定詞形放入結構化資料；需要語法、詞界或文件情境的判斷放入 `chinese_lint.py`；需要人工判斷的原則放入相應規則文件。
4. 加入至少一個應命中的樣本及一個語法相近但不應命中的反例。
   既有誤判應先成為回歸測試，再調整規則。
5. 執行相關單元測試及完整儲存庫檢查。
   新增規則識別碼、資料欄位或命令列選項時，同時驗證穩定輸出及錯誤情況。
6. 對影響生成文字或任務路由的變更進行前向測試。
   向獨立代理提供原始任務與材料，不提供預期答案、缺陷診斷或實作說明；比較修改前後的正確性、篇幅及資訊邊界。
7. 只有測試與前向測試都支持變更時，才更新版本及發布文字。
   未解決的反例不得以「人工審查」取代，除非規則本來就不能可靠地靜態判定。

## 變更對照

| 變更 | 先讀及修改 | 必要驗證 |
|---|---|---|
| 文字、語法、排版及註釋 | `references/wording.json`、`references/copy-fixtures.json`、`references/writing-policy.md`、`scripts/chinese_lint.py` | `test_chinese_lint.py`，再以目標樣本執行 lint |
| 技術及地區術語 | `references/technical-terms.json`、`references/lexicon-policy.md` | `lexicon_lookup.py`、`test_lexicons.py`、相關 lint 測試 |
| README、UI、寫作及發布語料 | 相應的 `*-corpus.json`、`*-style.md` 或工作流程文件 | `corpus_lookup.py`、`test_corpora.py`、`verify_corpora.py` |
| 內建或可選詞庫 | `references/lexicon-sources.json`、`lexicons/manifest.json`、`lexicons/ATTRIBUTION.md` 及授權檔 | `sync_lexicons.py --verify`、`test_lexicons.py`；可選詞庫另行驗證 |
| 觸發、工作流程及交付要求 | `SKILL.md`、相應規則文件、`evals/*.json` | `test_evals.py` 及不洩漏答案的前向測試 |
| 安裝、Action、pre-commit 及代理設定 | `install.sh`、`action.yml`、`.pre-commit-hooks.yaml`、`agents/`、`CLAUDE.md`、`opencode.json` | `test_install.py`、`test_integrations.py` 及 shellcheck |

先執行變更所屬的測試，再執行完整檢查：

```bash
python3 scripts/maintenance_report.py
python3 scripts/maintenance_report.py --json
python3 scripts/check_repository.py --release --network --require-shellcheck
```

`--network` 用於核對外部來源；一般文字變更不應依賴網路檢查。檢查失敗時先處理
失敗項目，不以維護報告的摘要代替原始診斷。

## 來源、語料與授權

- 優先採用專案維護者審查的第一方中文來源。
  通用規則至少需要兩個獨立來源支持；單一來源只能證明該專案的做法，或明確標為內部政策。
- 固定來源識別資料。
  資料包括完整 commit、檔案路徑、Git blob、版本日期、地區、用途、授權和不應採用的寫法。分支名稱及可變網址不能代替固定識別碼。
- 語料只保存來源資料與抽象模式，不複製原文。
  範例應自行編寫，且不得保留可識別的長句、翻譯或段落結構。
- 只有授權允許重新散布且歸屬資料完整時，才能打包快照。
  其餘來源只保留索引，或作為使用者主動安裝的可選資料。
- 詞頻、字形轉換及輸入法詞庫只能提供候選項或詞界證據。
  這些資料不能建立語法、語域或專業術語的權威性。
- 年代、專案規模或譯者資訊只能降低來源不確定性，不能證明文字未使用自動生成。
  無法核對作者與授權時，不應納入發布資料。

新增大型開源專案語料前，先指出現有資料無法回答的具體問題。只提取可重複使用的
句子功能、資訊順序或介面狀態；未形成新規則或新反例的整批語料不應加入儲存庫。

## 版本與發布

- 修正誤判、資料錯誤、來源資料或文件而不改變公開介面時，增加修訂版本號。
- 新增向後相容的規則、樣式、資料來源、查詢能力或整合時，增加次版本號。
- 公開介面出現不相容變更時，增加主版本號。
  這些變更包括移除或重新命名公開選項、規則識別碼、JSON 欄位或整合介面。無法確定相容性時，按較高等級處理。

發布前必須：

1. 同步更新 `VERSION`、`README.md` 與 `README.zh-CN.md`。
2. 將兩份 README 內的 pre-commit `rev` 和 Action `uses` 固定到新版本。
3. 提交候選內容後執行發布檢查，核對 `HEAD` 版本、工作目錄、來源、授權及 ShellCheck；該檢查會拒絕已修改或未追蹤的檔案。
4. 依 `references/pr-policy.md` 撰寫中文發布標題與說明。
   只記錄使用者可見變更、相容性限制及必要操作，不附命令清單、工作日誌或例行測試報告。
5. 建立標籤及 GitHub Release 後，核對標籤目標、發布版本、內容及公開連結。

已發布的標籤不得移動或覆寫。純文字錯誤可更正 GitHub Release；程式、資料、規則
或授權錯誤應以新的修訂版本發布，並在新版本中說明受影響範圍。

## 防止文件與實作漂移

- `SKILL.md` 只保存觸發條件、核心路由及最終檢查；詳細規則只保存在一份
  `references/` 文件或結構化資料中。
- README 只說明已發布的公開介面。命令列選項、樣式、整合輸入或安裝位置變更時，
  同一變更必須包含實作、測試及兩份 README。
- 保持規則、語料及來源識別碼穩定。重新命名時先提供相容路徑或升級主版本號，
  不以相同識別碼改寫既有含義。
- 每次發布前檢查文件中的路徑、選項及版本是否能由目前實作驗證；刪除失效說明，
  不為尚未實作的功能預先撰寫使用方法。
- 定期查看維護報告中的數量及一致性變化。數量增加本身不是改進；每項新增內容都
  必須對應問題樣本、證據、反例或可驗證的整合需求。
